{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ParseResult",
  "type": "object",
  "required": [
    "diagnostics",
    "dialect"
  ],
  "properties": {
    "diagnostics": {
      "description": "Parsing never fails — every anomaly accumulates here.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/Diagnostic"
      }
    },
    "dialect": {
      "$ref": "#/definitions/Dialect"
    },
    "encoding": {
      "description": "The WHATWG name of the encoding the detection chain actually decoded the input with (`\"UTF-8\"`, `\"EUC-KR\"`, `\"Shift_JIS\"`, `\"UTF-16LE\"`, ...; `encoding_rs::Encoding::name()`'s own values, which are WHATWG-standard labels). `None` only when no decode was attempted at all (the raw-byte oversize cap rejected the input before `encoding.rs` ever ran) -- `parse` (already-decoded `&str` input) always reports `\"UTF-8\"`, since that's the one encoding a Rust `&str` can ever be.\n\nThis is what makes the [`ByteSpan`] re-encoding caveat actionable: every span in this result is a byte offset into the UTF-8 text *after* decoding, so a consumer working with the **original** input bytes must decode them the same way first -- `new TextDecoder(result.encoding)` (Node.js/browsers both accept WHATWG labels directly), re-encode that decoded text to UTF-8, and slice spans against *that* buffer, not the original input bytes directly (see `wasm/README.md` for a worked recipe).\n\nBOM handling: for a document that opened with a byte-order mark, the mark is consumed during decoding and never appears in the decoded text -- spans are relative to the BOM-stripped content, same as every other span in this crate (offsets into what this crate's own decoding produced, not the original file's raw byte layout). A consumer re-decoding the original file with `TextDecoder` gets BOM-stripping for free (that's standard `TextDecoder` behavior for UTF-8/UTF-16 with a matching BOM), so no extra adjustment is needed on the consumer's side either.",
      "type": [
        "string",
        "null"
      ]
    },
    "mapper": {
      "description": "`None` when the root element is not a mapper/sqlMap (reason is reported as a diagnostic).",
      "anyOf": [
        {
          "$ref": "#/definitions/Mapper"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "definitions": {
    "ByteSpan": {
      "description": "Half-open range `[start, end)`: byte offsets into the UTF-8 text as decoded by this crate (identical to raw input bytes for UTF-8 sources; see the caveat below for re-encoded documents). B23 (cold code review): the previous headline -- \"in original bytes -- never in the decoded string\" -- directly contradicted its own caveat below for every non-UTF-8 input, since spans on a re-encoded document *are* offsets into the decoded string, not the original raw bytes.\n\nCaveat: this holds exactly for UTF-8 input, which decoding leaves byte-for-byte unchanged. For documents decoded from any other encoding (EUC-KR, Shift_JIS, GB18030, UTF-16, ... -- see `encoding.rs`, which supports every WHATWG encoding via a BOM/declared-label-driven chain, not just EUC-KR), decoding to UTF-8 changes byte *widths* per character, so spans on such documents are offsets into the re-encoded UTF-8 string, not the original raw bytes. This applies uniformly to every re-encoded document, not just Korean legacy files. Consumers reading spans back against a source file must decode that source the same way this crate did before slicing.",
      "type": "object",
      "required": [
        "end",
        "start"
      ],
      "properties": {
        "end": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "start": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        }
      }
    },
    "ClassRef": {
      "description": "Alias resolution is the consumer's job — only the raw text is kept.",
      "type": "object",
      "required": [
        "raw"
      ],
      "properties": {
        "raw": {
          "type": "string"
        }
      }
    },
    "ColumnMapping": {
      "type": "object",
      "properties": {
        "column": {
          "type": [
            "string",
            "null"
          ]
        },
        "property": {
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "DiagCode": {
      "description": "Additions only; removal/renaming is breaking. `#[non_exhaustive]` because new codes may appear within v1 itself (see `schema/README.md`) -- an exhaustive `match` outside this crate must add a wildcard arm, which is exactly the forward-compat behavior consumers need (an unrecognized code is not an error). `Other` covers the equivalent case for *deserialization* (this build reading JSON produced by a newer version).",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "encoding_undetectable",
            "encoding_mismatch",
            "unclosed_tag",
            "duplicate_statement_id",
            "missing_statement_id",
            "branch_limit_exceeded",
            "unknown_element"
          ]
        },
        {
          "description": "A **file-local heuristic**: this crate parses one mapper file at a time and has no view of any other file's `<sql>` fragments, so this is only emitted for MyBatis (`Dialect::Mybatis`), whose namespaces are per-file and whose refids are typically resolved within them. Never emitted for iBatis (B22, cold code review) -- iBatis fragments are a global cross-file registry by design (any sqlMap can reference any other sqlMap's `<sql>` by short name), so this heuristic would flag nearly every legitimate cross-file reference as dangling. Consumers that resolve `<include>` across an entire project (rather than one file) should treat a *missing* `DanglingRefid` as \"not checked here\", not \"resolved\" -- upstream MyBatis also supports cross-namespace short-name resolution that this single-file view can't see either, so even the MyBatis case is a heuristic, not a guarantee.",
          "type": "string",
          "enum": [
            "dangling_refid"
          ]
        },
        {
          "description": "Input exceeded [`crate::MAX_INPUT_BYTES`] (B25, cold code review: was only documented as \"the 10 MB cap\" in prose; now also a public, checkable constant). Emitted by both `parse`/`parse_bytes` (with `mapper: None`) and `detect_dialect` (with `Dialect::Unknown`) *before* any decoding is attempted, so the cap applies to raw input size regardless of encoding.",
          "type": "string",
          "enum": [
            "oversize_input"
          ]
        },
        {
          "description": "Recovery rule 3: first value wins, duplicate is reported here.",
          "type": "string",
          "enum": [
            "duplicate_attribute"
          ]
        },
        {
          "description": "An entity reference could not be resolved (e.g. `&nbsp;`, common in legacy mappers). The raw text is kept as-is for that segment (MM-08).",
          "type": "string",
          "enum": [
            "invalid_entity"
          ]
        },
        {
          "description": "A `#{`/`${`/legacy `#..#`/`$..$` placeholder never found its closing delimiter within the segment. The raw text is kept as-is (MM-07).",
          "type": "string",
          "enum": [
            "unterminated_placeholder"
          ]
        },
        {
          "description": "Recursion depth exceeded 256 nesting levels (dynamic-tag flattening or resultMap association/discriminator nesting) -- the remaining subtree is treated as opaque (no text/mapping contribution) rather than risk a stack overflow. Cold code review B2/B3, 2026-07-05.",
          "type": "string",
          "enum": [
            "nesting_limit_exceeded"
          ]
        },
        {
          "description": "An `<include>` is the first or last non-whitespace content directly inside a `<where>`/`<set>`/`<trim>` wrapper. MyBatis expands `<include>` *before* dynamic evaluation, so the wrapper's own leading-AND/OR or trailing-comma rule sees the fragment's actual substituted text; this crate flattens with the include token still in place, so a consumer substituting the fragment afterward must re-apply that rule themselves (and treat a wrapper whose only content is this include as conditional, since the fragment may expand to nothing). See the README's include section and `IncludeTarget`'s rustdoc. Cold code review A7, 2026-07-05.",
          "type": "string",
          "enum": [
            "include_at_wrapper_boundary"
          ]
        },
        {
          "description": "Forward-compat deserialization fallback: any code string this build doesn't recognize (e.g. JSON produced by a newer batis-xml version) lands here instead of failing to deserialize. Never produced by this crate's own parser -- hidden from docs since it's a deserialization mechanism, not a diagnostic you'd match on intentionally. Cold code-review contract audit, 2026-07-05.",
          "type": "string",
          "enum": [
            "other"
          ]
        }
      ]
    },
    "Diagnostic": {
      "type": "object",
      "required": [
        "code",
        "message"
      ],
      "properties": {
        "code": {
          "$ref": "#/definitions/DiagCode"
        },
        "message": {
          "type": "string"
        },
        "span": {
          "anyOf": [
            {
              "$ref": "#/definitions/ByteSpan"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "Dialect": {
      "description": "Closed set: an exhaustive `match` is a consumer feature (there's no forward-compat concern the way there is for `DiagCode`/`SqlText`, since `Unknown` already covers \"neither of the two known dialects\"). Adding a third dialect would be a v2.",
      "type": "string",
      "enum": [
        "mybatis",
        "ibatis",
        "unknown"
      ]
    },
    "IncludeRef": {
      "description": "A17 (cold code review, major): the include-token textual contract -- a stable part of the v1 output, documented here because nothing in the public docs said what the token actually looks like before this.\n\nEvery `<include refid=\"...\">` marker renders into the flattened [`SqlText`] as a SQL block comment: an opening `/`+`*`, the literal text `batis:include(`, this struct's own `raw` field, a closing `)`, then the closing `*`+`/`. `raw` is rendered verbatim -- the unparsed `refid` attribute value -- **except** any literal `*` immediately followed by `/` inside it is rewritten to `*` + `_` + `/`, so the token can never terminate its own enclosing comment early (a `refid` is untrusted XML attribute content, not something this crate controls the shape of). This holds regardless of `target`'s classification: `Local(\"frag\")` renders the comment around `frag` verbatim; `Qualified { ns: \"otherNs\", id: \"frag\" }` renders the *original, still-dotted* text `otherNs.frag` (`raw` is the whole unparsed attribute value; `ns`/`id` are just it split on the last dot for convenience, not a separate rendering); `Dynamic` renders the literal, unresolved `${...}` text as-is.\n\n**Locating tokens**: since the token's opening (`/`+`*` followed by the literal text `batis:include(`) is a fixed prefix, a plain substring search over the flattened SQL text finds every token directly -- no need to reconstruct it from `raw` first. Each token's position correlates 1:1 with one entry in the owning [`Statement::includes`]/[`SqlFragment::includes`] list: match by `Spanned::span`, which is the *original XML* span of the `<include>` element (not a position in the flattened text) -- the same span a [`DiagCode::IncludeAtWrapperBoundary`] diagnostic reports when this token sits at a `<where>`/`<set>`/`<trim>` boundary (see that variant's own doc comment, and the README's \"Include expansion order\" section, for the substitution contract itself).\n\n**Substituting a fragment with multiple variants**: a referenced `<sql>` fragment is itself flattened to a [`SqlText`], which may be `Variants` (several condition-gated alternatives) rather than one fixed string. There is no single deterministic substitution in that case -- the fragment's *own* active variant depends on the same runtime parameter state as the enclosing statement's variant does, so a consumer substituting fragment text into one variant of the parent statement must pick the matching variant of the fragment (by `conditions`), not an arbitrary one (e.g. `variants[0]`).\n\n**Document order**: [`Mapper::statements`] (and `fragments`/ `result_maps`) preserve source document order -- safe to assume when resolving forward/backward references across statements in one file.\n\n**Diagnostic messages are not a stable matching surface.** `message` strings may be reworded between versions without that being a breaking change; match on [`Diagnostic::code`] instead.",
      "type": "object",
      "required": [
        "raw",
        "target"
      ],
      "properties": {
        "raw": {
          "type": "string"
        },
        "target": {
          "$ref": "#/definitions/IncludeTarget"
        }
      }
    },
    "IncludeTarget": {
      "description": "Closed set: an exhaustive `match` is a consumer feature. `Dynamic` already covers \"can't be resolved statically\" -- there's no third kind of refid target. Adding a variant here would be a v2.\n\n## Expansion-order contract (A7, cold code review)\n\nThis crate never substitutes the referenced `<sql>` fragment's text in place of the `<include>` token -- resolving `IncludeTarget` to actual SQL and splicing it in is entirely the consumer's job. MyBatis/iBatis themselves expand `<include>` *before* evaluating `<where>`/`<set>`/ `<trim>` dynamic semantics, so a wrapper's leading-AND/OR strip or trailing-comma strip sees the fragment's real, substituted text. Flattening here with the token still in place means a consumer substituting fragment text in afterward must, at minimum:\n\n- re-apply the wrapper's leading-AND/OR / trailing-comma cleanup to the substituted text when the include token was first/last inside a `<where>`/`<set>`/`<trim>`, and - treat a wrapper whose only content is an include token as conditional (the fragment may expand to nothing).\n\n`DiagCode::IncludeAtWrapperBoundary` flags exactly the spots this applies to -- see the README's \"Include expansion order\" section for the full write-up.",
      "oneOf": [
        {
          "type": "object",
          "required": [
            "local"
          ],
          "properties": {
            "local": {
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "qualified"
          ],
          "properties": {
            "qualified": {
              "type": "object",
              "required": [
                "id",
                "ns"
              ],
              "properties": {
                "id": {
                  "type": "string"
                },
                "ns": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "`${}`-driven refid — marked unresolvable.",
          "type": "string",
          "enum": [
            "dynamic"
          ]
        }
      ]
    },
    "Mapper": {
      "type": "object",
      "required": [
        "fragments",
        "result_maps",
        "statements"
      ],
      "properties": {
        "fragments": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/SqlFragment"
          }
        },
        "namespace": {
          "description": "Usually `None` for iBatis sqlMaps (observed in the wild: the prefix lives inside the statement id itself, e.g. `WidgetDAO.getWidget`).",
          "anyOf": [
            {
              "$ref": "#/definitions/Spanned_for_String"
            },
            {
              "type": "null"
            }
          ]
        },
        "result_maps": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ResultMap"
          }
        },
        "statements": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Statement"
          }
        }
      }
    },
    "ResultMap": {
      "type": "object",
      "required": [
        "id",
        "mappings"
      ],
      "properties": {
        "extends": {
          "anyOf": [
            {
              "$ref": "#/definitions/Spanned_for_String"
            },
            {
              "type": "null"
            }
          ]
        },
        "id": {
          "$ref": "#/definitions/Spanned_for_String"
        },
        "mappings": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ColumnMapping"
          }
        },
        "span": {
          "description": "Full original extent: opening-tag start → subtree end.",
          "default": {
            "end": 0,
            "start": 0
          },
          "allOf": [
            {
              "$ref": "#/definitions/ByteSpan"
            }
          ]
        },
        "type_ref": {
          "anyOf": [
            {
              "$ref": "#/definitions/Spanned_for_ClassRef"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "Spanned_for_ClassRef": {
      "type": "object",
      "required": [
        "span",
        "value"
      ],
      "properties": {
        "span": {
          "$ref": "#/definitions/ByteSpan"
        },
        "value": {
          "$ref": "#/definitions/ClassRef"
        }
      }
    },
    "Spanned_for_IncludeRef": {
      "type": "object",
      "required": [
        "span",
        "value"
      ],
      "properties": {
        "span": {
          "$ref": "#/definitions/ByteSpan"
        },
        "value": {
          "$ref": "#/definitions/IncludeRef"
        }
      }
    },
    "Spanned_for_String": {
      "type": "object",
      "required": [
        "span",
        "value"
      ],
      "properties": {
        "span": {
          "$ref": "#/definitions/ByteSpan"
        },
        "value": {
          "type": "string"
        }
      }
    },
    "SqlFragment": {
      "type": "object",
      "required": [
        "id",
        "includes",
        "sql"
      ],
      "properties": {
        "id": {
          "$ref": "#/definitions/Spanned_for_String"
        },
        "includes": {
          "description": "Nested includes inside the fragment (MM-04).",
          "type": "array",
          "items": {
            "$ref": "#/definitions/Spanned_for_IncludeRef"
          }
        },
        "span": {
          "description": "Full original extent: opening-tag start → subtree end.",
          "default": {
            "end": 0,
            "start": 0
          },
          "allOf": [
            {
              "$ref": "#/definitions/ByteSpan"
            }
          ]
        },
        "sql": {
          "$ref": "#/definitions/SqlText"
        }
      }
    },
    "SqlString": {
      "description": "Flattened SQL text plus a mapping back to the source.",
      "type": "object",
      "required": [
        "span_map",
        "text"
      ],
      "properties": {
        "span_map": {
          "description": "(synthetic-text offset, original byte offset) segment-start pairs — strictly increasing.",
          "type": "array",
          "items": {
            "type": "array",
            "items": [
              {
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              },
              {
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              }
            ],
            "maxItems": 2,
            "minItems": 2
          }
        },
        "text": {
          "description": "Placeholders already normalized: `#{..}` → `?`, `${..}` → `__BATIS_DYN__`.",
          "type": "string"
        }
      }
    },
    "SqlText": {
      "description": "Result of dynamic-tag flattening (MM-06). Branch combination cap N=32 — total candidates per statement, computed as the cartesian product of tag branches.\n\n`#[non_exhaustive]`: a third fallback representation is plausible future work, and consumers matching exhaustively today must not break at compile time if one's added.",
      "oneOf": [
        {
          "type": "object",
          "required": [
            "variants"
          ],
          "properties": {
            "variants": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/SqlVariant"
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Over-cap fallback (accompanied by a `BranchLimitExceeded` diagnostic).",
          "type": "object",
          "required": [
            "union"
          ],
          "properties": {
            "union": {
              "type": "object",
              "required": [
                "branch_count",
                "text"
              ],
              "properties": {
                "branch_count": {
                  "description": "A **lower bound**, not necessarily the exact branch count: flattening bails out of cartesian expansion as soon as it's certain the total exceeds the cap, without finishing the (possibly much larger) exact count. Treat this as \"at least this many, over the cap\" rather than a precise total.",
                  "type": "integer",
                  "format": "uint32",
                  "minimum": 0.0
                },
                "text": {
                  "$ref": "#/definitions/SqlString"
                }
              }
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "SqlVariant": {
      "type": "object",
      "required": [
        "conditions",
        "text"
      ],
      "properties": {
        "conditions": {
          "description": "The `test` expressions (verbatim) that activate this variant.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "text": {
          "$ref": "#/definitions/SqlString"
        }
      }
    },
    "Statement": {
      "type": "object",
      "required": [
        "includes",
        "kind",
        "property_paths",
        "sql"
      ],
      "properties": {
        "database_id": {
          "description": "MyBatis per-vendor branching (`databaseId=\"oracle\"` etc.) — `None` when the statement doesn't declare one. Distinguishes otherwise duplicate ids (MM-03).",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/Spanned_for_String"
            },
            {
              "type": "null"
            }
          ]
        },
        "id": {
          "description": "`None` when missing, plus a `MissingStatementId` diagnostic. Synthesized ids are never invented.",
          "anyOf": [
            {
              "$ref": "#/definitions/Spanned_for_String"
            },
            {
              "type": "null"
            }
          ]
        },
        "includes": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Spanned_for_IncludeRef"
          }
        },
        "kind": {
          "$ref": "#/definitions/StatementKind"
        },
        "param_class": {
          "anyOf": [
            {
              "$ref": "#/definitions/Spanned_for_ClassRef"
            },
            {
              "type": "null"
            }
          ]
        },
        "property_paths": {
          "description": "Expression paths collected from `#{a.b}` / `${c}` (MM-07).",
          "type": "array",
          "items": {
            "$ref": "#/definitions/Spanned_for_String"
          }
        },
        "result_class": {
          "anyOf": [
            {
              "$ref": "#/definitions/Spanned_for_ClassRef"
            },
            {
              "type": "null"
            }
          ]
        },
        "result_map_ref": {
          "anyOf": [
            {
              "$ref": "#/definitions/Spanned_for_String"
            },
            {
              "type": "null"
            }
          ]
        },
        "span": {
          "description": "Full original extent of the statement: opening-tag start → subtree end (i.e. past its closing tag, or its own end for a self-closed element).",
          "default": {
            "end": 0,
            "start": 0
          },
          "allOf": [
            {
              "$ref": "#/definitions/ByteSpan"
            }
          ]
        },
        "sql": {
          "$ref": "#/definitions/SqlText"
        }
      }
    },
    "StatementKind": {
      "description": "Closed set: an exhaustive `match` is a consumer feature. `Generic` already covers \"some other statement-like tag with no CRUD-verb equivalent\" -- a new MyBatis/iBatis statement-like tag would be recognized by adding to `Generic`'s callers, not by growing this enum. Adding a variant here would be a v2.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "select",
            "insert",
            "update",
            "delete",
            "procedure"
          ]
        },
        {
          "description": "iBatis `<statement>` — a generic statement tag with no MyBatis CRUD-verb equivalent.",
          "type": "string",
          "enum": [
            "generic"
          ]
        }
      ]
    }
  }
}
