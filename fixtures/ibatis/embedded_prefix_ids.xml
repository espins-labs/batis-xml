<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
  "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap>
  <select id="widgetDAO.getWidget" parameterClass="long" resultClass="widget">
    SELECT widget_id, widget_name FROM demo_widget WHERE widget_id = #id#
  </select>
  <select id="orderDAO.getOrder" parameterClass="long" resultClass="order">
    SELECT order_id, widget_id FROM demo_order WHERE order_id = #id#
  </select>
  <update id="orderDAO.updateOrderStatus" parameterClass="map">
    UPDATE demo_order SET status = #status# WHERE order_id = #id#
  </update>
</sqlMap>
